#include "bits/stdc++.h"
using namespace std;

int minHeight(int arr[],int k){
    int n= sizeof(arr)/sizeof(arr[0]);
    sort(arr,arr+n);
    int min=arr[0];
    int diff = INT_MAX;
    int max=arr[0];
    int dp[n+1];
    // [1,4,5,9,11] k=5
    for(int i=0;i<n;i++){

    }

}
int main(){

}